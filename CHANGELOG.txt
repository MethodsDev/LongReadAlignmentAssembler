v0.18.2, in development
- fixed `num_total_reads`, the denominator behind every TPM and behind `min_monoexonic_TPM`, depending on the alignment policy applied to the BAM. The count was taken from whatever `_filter_bam_for_secondary_alignment_policy()` returned, so `--secondary_alignment_mode all` counted every record in the input while `heuristic` counted the filtered file, from which that function drops unmapped and supplementary records before considering any secondary alignment. On an SG-NEx ONT cDNA BAM that is ~74% unmapped, the two modes gave 9,480,826 and 2,436,734 for identical input, a 3.89x shift that moved the monoexonic threshold from a ~9.5-read floor to a ~2.4-read floor and quadrupled the number of single-exon models reported. The count is now taken from the original BAM before preprocessing and restricted to genome-mapped reads (`-F 0x904`), so each aligned read contributes once and the value no longer varies with `--secondary_alignment_mode`. The WDL `count_bam` task, which supplies the same value to every shard on the scatter path, applies the identical filter. The cache filename records the counting policy so a `.count` written under the previous semantics is not reused.
- transcriptome rescue now requires at least 95% of a read's length to align to the target transcript (`rescue_unassigned_min_aligned_read_frac`). Percent identity is computed over the aligned portion alone and is blind to clipping, and the pre-existing check that a rescue explain at least as much of the read as its genome alignment applies only to reads that had a genome alignment to begin with; reads with no graph path, which are the bulk of what rescue retries, had nothing bounding how much of the read the target accounted for, so a short stretch of a long read aligning at the identity floor counted as support for the whole isoform. The requirement is aligned length over read length rather than matched bases so that platform error rates do not make it unsatisfiable; mismatches remain bounded by `rescue_unassigned_min_per_id`.
- monoexonic isoforms must now show that their supporting reads describe one molecule rather than a covered region. A single-exon model has no intron chain corroborating it, so reads that tile a span without overlapping could be collapsed into one long transcript; on SG-NEx MCF7 chr17 this produced, among others, a 19,721 bp single-exon model supported by 188 reads of median length 514 bp. Two criteria are added, each disabled by setting it to 0: `min_monoexonic_read_span_peak_frac` (default 0.5) requires that fraction of the supporting reads to mutually overlap at some single base, and `min_monoexonic_adjusted_TPM_ratio` (default 0.2) requires the model's coverage-depth TPM, read bases on the model divided by model length, to reach that fraction of its read-count TPM. The ratio equals the mean fraction of the model an individual read covers, so it is independent of library size and read count and measures agreement rather than abundance; an absolute threshold on the coverage-depth TPM does not substitute, since a long model tiled by many reads still averages several-fold depth.
- matched reconstructed isoforms to final-graph reference paths after removing reference TSS/PolyA nodes, preserved the matching reference IDs across quantification resets, and excluded synthetic reference reads from draft expression estimates.
- fixed the post-assembly draft quantification step to rescue reads against the assembled isoforms being quantified instead of the supplied reference models; the mismatch let reads already assigned to a draft isoform be realigned onto a reference path and counted twice in the read support that drives isoform filtering.
- fixed transcriptome rescue losing its low-percent-identity candidates on an alignment cache hit: the discarded read names are now stored beside the cached alignments and restored when the cache is reused, and a cache lacking that record is re-extracted rather than reported as having no discards. The cache key also covers the extraction flags that determine what gets discarded, and the unrestricted alignment pickle no longer shares a filename with the multi-exon subset.
- fixed transcriptome rescue reading a truncated read sequence when a hard-clipped supplementary record preceded the primary alignment; sequences are now taken only from primary records.
- the final cross-gene EM correction now runs with the configured EM settings rather than the helper script's own defaults: regularization follows `EM_alpha`, and the abundance floor and unique-read fraction are configurable. Both EM passes now share one convergence rule, `EM_convergence_tol`, applied as the L2 change in normalized abundances, and the cross-gene maximization step no longer adds the abundance floor to its update, so the two passes optimize the same objective. Unique-read counting is defined in one place per purpose: reporting requires a whole read (fraction 1.0) in both the per-locus and cross-gene outputs, while isoform filtering counts a read as unique at 0.9995 to tolerate EM rounding.
- per-read `frac_assigned` and `read_weight` values in the quant tracking output now carry six decimals instead of three. The cross-gene correction re-reads those columns as EM weights and re-derives counts for untouched transcripts from them, so the previous rounding both perturbed the model and drifted reported counts.
- fixed a `ZeroDivisionError` in degradation-product pruning: the relative share of a transcript pair was computed before the pair was tested for structural eligibility, so a gene with expression elsewhere that also contained two zero-support models aborted the run. Such a pair is now left out of expression-based pruning, since its relative support is undefined rather than zero.
- annotation-free mitochondrial pseudo-genes emitted under `--oversimplify` are now named `g:<contig>:<strand>:OVSIMP` rather than `g:OVSIMP:<contig>:<strand>`, so they match the single-cell mitochondrial pattern used for `percent.mt`. Previously those pseudo-genes contributed nothing to mitochondrial content and the cell-level mitochondrial filter never fired in annotation-free runs.
- short annotated transcripts are no longer silently dropped during discovery. A multipath component below `min_transcript_length` is retained for assembly when it holds an input-transcript path, and the transcript-length filter exempts an expressed model that contains a reference transcript. Previously such a locus produced no candidate isoform, so its reads matched no isoform at quantification and were discarded without an output row: on the SIRV reference-guided test, the two annotated transcripts under 200 bases (161 and 189 bases) lost all 15 of their reads, which this fix restores to the reported totals. Those totals no longer account for every read in the BAM: the rescue aligned-length floor added later leaves SIRV107's 11 reads and SIRV206's 3 unassigned, neither model being supplied by the half annotation nor reconstructable from reads, so the test pins 537 of 551.
- gene and isoform identifiers are now reproducible across runs. `comp-N` and `iso-M` were position counters over data structures keyed by splice-graph node and multipath objects, which hash by identity, so set iteration order and therefore the numbering varied between processes. Clusters and isoforms are now numbered from genomic structure, with `comp-1` the leftmost locus on the contig and strand, and reclustering sorts its input so gene membership no longer depends on reconstruction order. Component discovery, the final report order, the splice-containment columns of `quant.expr`, and the BED converter are all ordered deterministically as well. On the SIRV reference-guided test, `quant.expr`, `quant.tracking`, the GTF, and the BED are now byte-identical across repeated runs.
- read paths that match no gene in the splice graph are now counted and reported. Such paths were appended to a list that was never read, so their reads left quantification with no output row, no warning, and no rescue attempt; the only trace was the gap between two other counters. The quantification audit now reports `num_paths_unanchored` and `num_read_counts_unanchored`, and a warning names the lost read count when it is nonzero. Reporting only; no assignment behavior changes.
- reference-guided isoform discovery no longer rescues reads that already have a path through the splice graph. Transcriptome rescue drew candidates from two sources: reads with no graph path, and reads that failed to match a rescue target. During discovery the targets are the supplied reference models, so the second source selected precisely the reads that evidence a novel isoform, realigned them against reference cDNA, and projected them back as a second multipath shaped like a reference model. Extracting the reference path then claimed those reads, which zeroed the genuine novel path's score and ended path extraction with the novel model never built. Discovery now rescues only reads with no graph path; the post-assembly quantification callsite is unchanged, where the targets are the models being quantified and the question is the right one. On the SIRV reference-guided test supplying half the annotation, exact recoveries rise from 60 to 67 of 69 and reported reads from 525 to 537, and guided recovery becomes a superset of annotation-free recovery under matched settings.
- the synthetic reads standing in for input transcripts no longer count toward path scores during isoform discovery. Each input transcript is injected as a multipath carrying one `reftranscript:<id>` read, which gives the reference structure a template in the graph; that read was also counted as support, so every supplied model entered path selection with one unit of manufactured evidence that no novel model could have. A model with no real reads therefore scored 1, cleared `min_path_score`, and was selected as a candidate on nothing but its own annotation. Scoring now ignores those reads, so such a path scores zero and is never selected, while a model with real reads competes on exactly its read count. The read itself is retained on the multipath, so it still supplies the template, satisfies the graph's minimum node count, keeps short annotated loci eligible for assembly, and carries provenance. The node-count fallback in path scoring is now reached only when no read IDs exist at all, rather than whenever a score came out zero.
- graph nodes left unrepresented by isoform path extraction are now reported. Extraction stops when the best remaining path falls below `min_path_score`, which can end a component with nodes still awaiting representation and nothing said about them. The summary now separates the two cases: a node supported only by the synthetic read for an input transcript is expected to be left behind, since nothing observed it, while a node carrying real reads means those reads describe a structure no reported transcript covers, and that raises a warning. Reporting only; extraction is unchanged.
- retention of internally primed monoexonic isoforms is now configurable. A 3' end agreeing with a reference annotation argues that a polyA signal is genuine rather than internal priming, and multi-exonic models were already spared on that basis; monoexonic models were filtered unconditionally without consulting the known 3' ends at all. Setting `spare_monoexonic_internal_priming_with_known_3prime` gives monoexonic models the same reprieve. It defaults to False, preserving current behavior, because a monoexonic model carries no intron chain corroborating it and 3' agreement is weaker evidence there.
- removed the `collapse_alt_TSS_and_PolyA` mode. It gated collapsing reconstructed isoforms that share an intron chain but differ at their termini, and, in degradation pruning, subsuming any splice-compatible contained isoform instead of applying the TSS/PolyA eligibility rules. The key defaulted to False and its only hook was commented out, so neither behavior was reachable; the eligibility rules that already ran are now the only path.
- read junctions declined by the splice-motif check are now counted and reported per contig and strand, separating a motif that implies the opposite strand from one that is canonical in neither orientation. The second case previously produced no record at all. A read whose junctions are all declined has no path through the splice graph, so a locus reachable only through such junctions cannot be reconstructed from reads, and that was previously visible only as an unexplained count of reads with no graph path. Annotation-supplied junctions do not pass through this check, so such a locus is still reachable when the model is provided. Reporting only; junction admission is unchanged.
- degradation-product pruning now resolves equal-length models deterministically, on read support. Transcripts were ordered by descending cDNA length, but that sort is stable and `Transcript` hashes by identity, so models of identical length stayed in the arbitrary order their set produced. Terminal variants of one intron chain are exactly that case: each qualifies as the other's degradation product, and the structural tests run before the expression test, so whichever was examined first survived and absorbed its partner. The survivor therefore changed between runs, and could be the worse-supported model. Ties on cDNA length now break on assigned reads and then on structure, so the better-supported model wins; read counts are a stable snapshot, since absorbing transfers multipath evidence rather than assigned counts. On the SIRV reference-guided test this moves one locus, whose terminals now follow the twin carrying 3 reads rather than the one carrying 2.
- transcriptome rescue now declines an alignment that explains less of a read than the read's own genome alignment already does, and no longer refuses to consider a read merely because it has a graph path. The previous guard, which skipped every read that had any path, was too blunt: a read whose true structure contains a microexon is mapped to the genome with the tiny exon emitted as an insertion rather than traversed, so it has a path, that path is wrong, and only realignment against the transcript recovers it. Rescue is additive and never replaces a read's original multipath, so the question that matters is not whether a read has a path but whether the rescue improves on it; explained read bases answer that, while percent identity does not, being measured over the aligned portion and therefore blind to a target that soft-clips its disagreement away. Reads with no usable graph path keep their exemption, since rescue is their only route in and their genome alignment may still look good.
- fixed transcript-merge mode producing an empty GTF. Discovery injects each input transcript as a template read and discounts it from path scoring, so that a model no read supports cannot be selected on its annotation alone. That registration was not scoped to discovery, and merge mode has no real reads at all: its fake reads carry each input model's weight and are the only evidence present, so discounting them scored every path zero and merged nothing. Registration now happens only on the discovery branch. On the merge execution test the two chr19 strands go from 0 transcripts to 297 and 220.
- an input-transcript template now breaks a tie in isoform path selection. A template contributes nothing to a path's score, so it still cannot make an unsupported model selectable; it only settles which of two paths carrying the very same reads is taken. That case is real: a read whose genome alignment misplaces a microexon, emitting it as an insertion rather than traversing it, supports both the path it was misaligned to and the annotated path it is rescued onto, at identical read counts. Whichever was examined first used to win, and since the template stopped counting toward the score there was nothing left to separate them, so a 4-base microexon that earlier releases recovered was lost. On the SIRV reference-guided test the output is byte-identical.
- a rescued read is now represented only by its rescued path. Rescue realigns a read against a target and projects the hit back into the graph, but it did not detach the read from the path it already had, so one read supported two competing structures and a path whose reads had all moved elsewhere kept their support. On the microexon execution test the same 570 reads backed both the misaligned path and the corrected one at identical counts, which is what made them tie. Acceptance has already established that a rescued alignment explains at least as much of the read as its genome alignment, so the original attachment is the one to drop; a path left with no reads is removed. Reads with no usable graph path have nothing to detach. This also removes at the source the duplication behind the earlier draft-quantification double counting, where a read reaching two surviving models was counted on both.
- the `# LRAA version <v>` comment line now heads every key output, not just some of them. The GTF, `quant.expr`, and `quant.tracking` already carried it; the BED and the two `*.pre-cross-gene-EM.quant.*` files did not. The BED is generated from the finished GTF by an external converter, and the pre-correction quant files are moved aside before the cross-gene EM runs, so both bypassed the stamping applied to the final quant outputs. Every reader in the tree already skips leading `#` lines, including the BED converter, the cross-gene EM helper, and the test validators.
- key outputs now record the invocation that produced them, as a second comment line `# LRAA CMD: <argv>` beneath the version line. The command is shell-quoted, so it parses back to the exact argument vector and can be pasted into a shell to reproduce the run. It is the command as given, not the effective configuration: presets such as `--HiFi` and the contents of a `--config_update` file stay implicit. The version line is left byte-identical so the exact-match idempotency guards in the WDL still recognize a stamped file, and every reader in the tree skips all leading `#` lines rather than a fixed count. In sharded WDL runs each shard records its own command; the merged outputs summarize them (see below).
- WDL merged outputs now carry a command header derived from the inputs they were built from. A sharded run has no single command -- each shard names its own split genome, split BAM and `--contig` -- so `util/lraa_merge_header.py` reduces the shard commands to what they agree on, writing one `# LRAA CMD:` line with the arguments that differed marked `<varies>`, followed by `# LRAA merge: <n> inputs; ...; --contig values: ...`. Nothing is synthesized: when the shard commands disagree on a flag rather than a value, every distinct command is emitted verbatim instead. This covers the merged GTF, the merged `quant.expr` and `quant.tracking.gz`, their `pre-cross-gene-EM` counterparts, and the cell-cluster-guided merged GTF and tracking.
- fixed the WDL GTF merge leaking each shard's `# LRAA CMD:` line into the body of the merged GTF. It stripped only the first line of each shard, which was the version comment until the command line was added beneath it, so a merged GTF opened by claiming the whole file came from `--contig chr1` and repeated a stale command at every shard boundary. Each shard's entire leading comment block is now dropped in favor of the merged header.
- a configured transcriptome alignment now fails when minimap2 is absent instead of quietly not happening. Rescue is on by default, and the only sign that it had been skipped was a warning written to a per-contig log that the run deletes on cleanup, so a run missing the binary reported fewer assigned reads and looked successful: the Docker image shipped without minimap2, and on the SIRV reference-guided test that cost 11 of 548 assigned reads with nothing in the retained output to say so. `LRAA` now checks for the binary immediately after the run configuration is settled -- when rescue is enabled or `--quant_read_assignment_mode` is `transcriptome_only` or `genome_tx_arb` -- and exits before any work is done, naming the setting that requires it; the rescue entry point raises rather than returning empty for callers that bypass the CLI.
- the Docker image is 3.95GB, down from 7.41GB, and now carries minimap2. Nothing was removed that anything reaches: `libghc-zlib-dev` pulled the 667MB GHC toolchain for a Haskell zlib binding the C builds never wanted (`zlib1g-dev` is what they use), Seurat installed with `dependencies = TRUE` brought ~620MB of Suggests led by BPCells, the full NCBI BLAST+ release cost 672MB where TransDecoder's blastp path runs only `blastp` and `makeblastdb`, the GDAL/GEOS/PROJ stack cost ~400MB with no spatial R package installed, `default-jre` cost ~200MB with no java caller, and pip and R caches, samtools/htslib source trees, apt lists, `devtools`, and the repository's own `testing/` fixtures accounted for the rest. The LRAA checkout is now the pinned commit's source archive rather than a clone, since no runtime code consults git.
- the R install layer now fails the build when a package it names is missing. `BiocManager::install` only warns when a dependency cannot be built, so a layer could exit 0 having installed nothing: `clustermole` needs GSVA -> SpatialExperiment -> magick, magick needs libmagick++-dev, and no layer installed it, so the package has been absent from every image built while appearing to install. It is now left out deliberately -- only `util/sc/notebook_templates/Seurat_get_cell_clusters.Rmd` loads it and no workflow renders that notebook.
- added a split image family beside the single image: `Docker/Dockerfile.core` (422MB) carries what an LRAA run reaches -- python with pysam/networkx/intervaltree/numpy/igraph, samtools, htslib, minimap2, gffcompare, perl -- built in two stages so no compiler or header ships. `Docker/Dockerfile.orf` (615MB) adds TransDecoder, diamond and the two BLAST+ programs its blastp path runs; `Docker/Dockerfile.sc` (2.69GB) adds the R stack (Seurat, DropletUtils, tidyverse, edgeR) and scientific python for the util/sc and util/misc helpers. Both derive `FROM` the core image, so a workflow that needs one pulls only the layers above what it already has.
- every WDL task now names the image its commands actually need. 18 of the 27 tasks run on `lraa-core`, 8 on `lraa-sc` (anything invoking `Rscript`, plus the helpers importing pandas/scipy), and 1 on `lraa-orf`. The existing `docker` input keeps its name and means the core image; `LRAA-singlecell.wdl` and `LRAA-cell_cluster_guided.wdl` gain a `docker_sc` input for their R-bearing calls, and the outer workflow passes its value down so the nested single-cell task inherits the caller's choice. `count_bam` and `validate_pre_normalized_inputs` no longer hardcode an image and take it from their call sites. WDL defaults use `:latest`, `testing/` input JSONs use `:testing`, and both build scripts now build all four images -- core first, since the other two are `FROM` it -- tagging the versioned build `<version>` and `testing`.
- the plain `lraa` image name is now an alias for `lraa-core`, and the combined image moved to `lraa-combined`. We pay egress on every pull, and someone pulling `lraa` to assemble isoforms was fetching 1.45GB of compressed R and TransDecoder layers to run a splice graph; the same pull now transfers 139MB. `docker tag` gives `lraa` and `lraa-core` one digest, so the alias costs no extra build and no extra storage. `make_simg.sh` names `lraa-core` explicitly.
- the images build from a shared `lraa-base` instead of deriving from each other, so a version bump no longer recompiles Seurat. `lraa-sc` and `lraa-orf` were `FROM lraa-core`, which put their R and TransDecoder layers above core's LRAA checkout; since Docker keys a layer's cache on its parent image id, moving the pinned commit invalidated everything above it and a release that changed nothing but a git SHA took an hour. All three now build from a dependency-only base with the checkout as their final layer: a simulated bump rebuilds all four images in 20 seconds. The pinned commit moved out of the Dockerfiles into `Docker/LRAA_CO.txt` and is passed as a build arg, so one file holds the SHA rather than four that can drift apart.


v0.17.7, June 28, 2026
- fixed `mergeQuantResults` WDL disk allocation for cross-gene EM: the merged tracking is decompressed for external sort, so disk now scales as 60x the compressed tracking size (15x decompression x ~4 on-disk sort/temp copies) plus 2.2x the expr size, replacing the prior insufficient compressed-size-based formula.
- added standard isoform expression matrices with unique-isoform modes to `build_LRAA_expr_matrices.py`.
- reworked `collapse_LRAA_GTF_by_splice_pattern.py` to group by `(gene_id, splice_pattern)` instead of intron-hash alone, matching `build_LRAA_expr_matrices.py` keying and preventing cross-gene merges at overlapping/antisense loci; also emits `<output_gtf>.isoform_merge_report.tsv` and `<output_gtf>.gene_conflicts.tsv` reports.
- made gene-symbol assignment strand-aware in `gene_symbol_utils.py` by skipping antisense gffcompare class codes (`s`, `x`) via a new `disallowed_class_codes` parameter, so a symbol is no longer assigned to a transcript on the opposite strand.
- enabled grouping by gene symbol in the edgeR DTU runner (`run_edgeR_DTU.R`).
- reduced memory usage of the feature-length plotting summarization script and restored its plotting compatibility.
- expanded prerelease testing, including a same-gene quant pre/post regression test and a comprehensive `make all` target.
- LRAA now writes a version header into its output files, propagated through the WDL workflows and downstream utilities.


v0.17.5, May 18, 2026
- added final cross-gene EM reassignment for retained secondary/multi-gene read assignments, preserving pre-correction `*.pre-cross-gene-EM.quant.expr` and `*.pre-cross-gene-EM.quant.tracking` outputs while reporting cross-gene-corrected quantification as the primary result.
- refined secondary-alignment handling by making the secondary-rescue heuristic the default policy, preprocessing BAMs to retain all mapped non-supplementary primary alignments plus only heuristic-supported secondary alignments, and exposing secondary-rescue BAMs and summaries through WDL outputs.
- added genome/transcriptome quant-read assignment arbitration support, including `genome`, `rescue_unassigned`, `transcriptome_only`, and `genome_tx_arb` assignment modes, genome-gated transcriptome target construction, and merged `*.genome_tx_arb.summary.tsv` audit outputs.
- improved transcriptome-rescue robustness and reporting, including rescue-unassigned summary logging, documented transcriptome read-rescue path construction, transcriptome rescue execution tests for microexon and repetitive-gene cases, and better handling of BAMs without `AS` tags or with tied best-AS alignments.
- standardized LRAA output naming with mode-specific suffixes (`LRAA.quant-only`, `LRAA.ref-guided`, `LRAA.ref-free`) and aligned scattered/WDL output names, caching behavior, and merged-summary handling.
- updated quant tracking output to always include read weights, supporting downstream cross-gene EM correction and tracking-based sparse-matrix generation without requiring debug mode.
- removed legacy pre-cross-gene EM WDL handling, exposed transcriptome-arbitration and pre-cross-gene-EM artifacts in WDL outputs, and added/updated regression tests for multigene secondary quantification, quant read-assignment arbitration, secondary-rescue filtering, and txalign-path rescue.


v0.17.0, May 11, 2026
- added transcriptome-based read rescue for unassigned reads in quantification. Rescue now runs before the first EM in quant-only mode and can recover reads discarded as `DISCARDED-SPACER`, reads anchored to a gene but incompatible with annotated isoforms, and reads dropped at genomic alignment parsing for low percent identity when they align cleanly to local transcript sequences.
- extended rescue into reference-guided isoform identification for multi-exon annotation-supported transcripts so rescued reads can contribute during guided isoform assembly and survive downstream filtering, not just the final quant step.
- made read rescue default-on and added `--no_rescue_unassigned_reads_via_transcriptome_alignment` as the opt-out CLI parameter. The same master switch now disables rescue in quant-only, final quant during isoform identification, and guided assembly rescue.
- made secondary alignments default-on, lowered default `min_mapping_quality` and `min_mapping_quality_for_final_quant` to `0`, and added `--no_allow_secondary_alignments` as the opt-out CLI parameter. Retained hidden backward-compatible positive CLI aliases for the old opt-in forms where needed by tests and older wrappers.
- updated rescue mode behavior to respect sequencing mode defaults: rescue `min_per_id` now follows the active run setting, and rescue minimap2 presets default to PacBio HiFi-oriented mapping in `--HiFi` mode and ONT-oriented mapping otherwise.
- exposed rescue control through the WDL workflows and Terra example configs, with default-on behavior propagated through bulk, clustered, and single-cell entry points.
- fixed handling of BAM records lacking `SEQ` on retained non-primary alignments by skipping only sequence-dependent soft-clip refinements while still allowing those alignments to contribute structurally.


v0.16.1, Apr 29, 2026
- stabilized LRAA-gtf-merge boundary provenance handling and ensured boundary read counts are consumed correctly during merge; added/updated boundary-count tests and HiFi/non-HiFi merge fixtures.
- enforced `min_transcript_length` on final assembled isoforms to ensure short transcripts are filtered at final output.
- improved CLI guidance for sequencing modes by adding a HiFi vs non-HiFi help table and introducing an override for splice-boundary aggregation behavior.
- updated merge_LRAA_GTFs regression test inputs/checks, including refreshed PBMC HiFi-oriented fixture sets and annotation expectations.
- updated single-cell WDLs to support reusable pre-normalized cluster BAM paths and added a cluster-guided quant-only full-pipeline test configuration.



v0.16.0, Apr 27, 2026    
- adjusted HiFi quant read assignments by snapping read paths (not transcript paths) to nearby terminal TSS/PolyA features when soft-clipping and splice-graph adjacency support it.
- updated quant defaults and scoring: normalized TPM after merge - sum to 1M, softened 3' end weighting near alternative PolyA windows, enabled transcript-boundary splice-graph fracturing by default, switched to two-stage mapping-quality filtering, raised default `max_intron_length` to 200000, lowered HiFi `min_per_id` to 97, and defaulted candidate PolyA site pruning to minimum isoform fraction.
- fixed transcript filtering/annotation issues, including boundary remap + duplicate-pruning behavior, bugfix wrt nonsensical InternalPriming=True annotation on retained multi-exon transcripts, and `min_transcript_length` edge cases; added TSS/PolyA boundary read counts to GTF output.
- added ORF workflow support via `WDL/LRAA-ORF-prediction.wdl`, including BLAST/DIAMOND homology integration, corrected ORF output naming, and transdecoder output directory export.
- added edgeR-based DTU runner with plotting (`util/run_edgeR_DTU.R`) and updated DTU docs/tests.
- improved single-cell matrix generation with a faster `util/sc/singlecell_tracking_to_sparse_matrix.py`, safer sparse-matrix CSV defaults, ENSG-to-symbol mapping for LRAA features, and CAS input-matrix workflow output.
- improved WDL/Terra robustness with dynamic BAM-based memory sizing, higher scattered shard memory multipliers, `min_disk_GB`, better optional `File?` disk handling, and bounded progress-log tailing.
- expanded test coverage, including HiFi boundary regression cases, boundary-count tests, quant TPM/reporting invariants, filtering regressions, DTU tests, ORF WDL tests, and main pytest integration in `testing/make`.


v0.15.0, December 14, 2025
- using 90th percentile for trimming non-TSS and non-PolyA termini based on read boundaries


v0.14.0, December 6, 2025
- in HiFi mode, only retain single-exon transcripts if they have TSS or PolyA annotated
- incorporating filtering of good cells via DropletUtils and revised sparse matrices limited to good cells, and made filtering good cells optional
- support quant-only mode for the single cell workflow
- CAS integration as plugin for cell type annotations
- bugfix for DTU anlysis taking into account cell fractions: enforce the min cell fraction requirement on the higher delta pi value cluster for the isoform. Also, if excluding unspliced, remove them from contributing to the isoform fraction dataframe too
- include the umap and seurat objects as wdl outputs in the single cell pipe
- under LRAA merge mode, report problematic transcripts that dont have paths in the splice graph, no longer a fatal error.


v0.13.0, November 23, 2025
- restructured WDLs for single cell pipeline, introducing 'basic' vs. 'cluster-guided' modes, removed the --bam_list from LRAA and relying on wdl pipe for orchestration of cluster-based quants
- improved logging and mulithreading in workflows


v0.12.7, November 18, 2025
- merge_LRAA script allows for alternative backends but defaults to in-memory
- bugfix: MultiPath assumed every read name could be resolved to a compact ID via READ_NAME_STORE. On Terra’s merge job that store failed to initialize, so the fake provenance names for input transcripts never mapped to IDs, _read_count stayed zero, and MultiPathCounter.add tripped assert multipath_obj.get_read_count() > 0. We hardened MultiPath to fall back to deterministic hash-based IDs when the store is unavailable and to seed the count from the provided read_names, so the counter always sees a positive weight even in store-less merge mode.
- added wdl progress monitoring to cell cluster-guided lraa
- allow for gene_id and transcript_id in ref_gtf to be separated by other metadata
- sparse matrix builder - updating to avoid the segfault pandas bug, use python engine


v0.12.0, November 14, 2025
- caching initially constructed splice graphs for reuse in downstream quants to avoid reconstruction effort.
- all splice graphs for all steps leverage the normalized read coverage (not just isoform ID)
- added --num_parallel_contigs to control contig-level concurrency (default 4 workers); --num_threads_per_worker (legacy --CPU alias) specifies cores allocated per contig/strand worker (default 1 core)
- default --oversimplify now targets mitochondrial contigs ("chrM,M")
- wdls optimized for splitting data per chromosome
- scatter runs per chromosome only (strand-level splitting removed) with subjobs shuffled for improved load balancing
- including ref-annot gene symbol integration into single cell pipeline


v0.11.0, November 10, 2025
- LRAA wdls to scatter over the chromosome/strand combo
- improved multithreading within contigs
- more logging and progress monitoring for long running parts
- using Leiden alg for isoform-to-gene clustering and adjustments for large clusters to avoid runtime performance issues
- added --verbose to force direct logging to the terminal. When running in default parallel contig mode, all logging info is written to the corresponding chr/strand err log

    
v0.10.0 November 2, 2025
- Defaults now tuned for non-HiFi long reads (e.g., ONT). Replaced --LowFi flag with --HiFi to enable more stringent PacBio HiFi parameters.
- HiFi mode sets: min_per_id=98, infer_TSS/PolyA enabled, max_exon_spur_length=13, no splice-boundary aggregation, and fractures splice graph at input transcript bounds.
- runs --CPU parallel contig/strand combinations. If --no_parallelize_contigs, then allows up to --CPU per cluster of reads in per contig/strand evals.
- can resume from earlier partially completed runs.  Disable with  --no_resume_parallel
- uses SQLITE (or lmdb) where needed to reduce RAM usage.  See --store_backend
- new --bam_list option intended for quant-only runs with single cell cluster data, each bam corresponding to the cell cluster partitioned bam. Used in single cell pipeline (WDL).


v0.9.0 Sept 26, 2025
- trimming transcript UTRs to min/max of contained evidence transcripts and only adjust UTRs to min/max of contained isoforms if the corresponding polyA or TSS annotation flags arent set


v0.8.0
- ME and SE distinct phases followed by integration
- classify known isoforms by splice pattern hash (introns) matching input reference set; ME/SE or mono-exonic isoforms always treated as novel for min_reads_novel_isoform filtering
- including gtf annotations for likely internally primed ME isoforms.
- restructured pretty alignment extraction and caching into Pretty_alignment_manager.py

    
v0.7.0
- rework single cell ref-guided and denovo pipes so isoform ID happens at the cluster-level, but quant is more simply performed post gtf-merge step in pseudobulk-all mode
- updating the LRAA_globals to "max_dist_between_alt_TSS_sites": 50 from earlier zero setting
- adding wiki as submodule
- adding splice pattern eval support: splice_patterns_tsv_to_bed.py, segment_LRAA_GTF_by_shared_exon_intervals.py
- adding diff iso usage analysis notebook template including isoform structure, expression, and umap expr
- making LRAA runner less verbose in WDL for Terra batch API compatibility


v0.6.0
- incorporate splice pattern codes into expr.quant and tracking files
- include splice pattern based sparse matrix
- update annotated features in sparse matrices including splice patterns and include separate id mapping file with new vs. old ids.
- WDL update for sc-cluster-guided to separately tar tracking and gtfs for init vs. final
- sc-cluster-guided gtf merging improvements for handling alt TSS and polyA
- including quant-only option for single cell cluster-guided pipe


v0.5.0
- cache pretty alignments for reuse

v0.4.0
- overhaul to leverage the multipaths during quant instead of the individual read assignments.
- expose options for how single cell data are tagged in bam files. Default are CB,XM tags for cell barcodes and umis.


v0.3.0
- public release after testing


v0.2.37
- refined SQANTI-like categories, including code-level tests and documentation
- refactored EM code
- stricter rules around polyA site identification
- simplified partial isoform filtering logic
- setting max spur length to 13 instead of 5
- reset exon coverage vals post pretty alignment corrected alignments stag
- added region extractor utility for troubleshooting individual regions or genes in minigenome-style
- disabling MAPQ=0 by default
- incorporating a unique read support rule for novel isoforms

v0.2.36
- new sqanti-like classes updated w/ tests and better resolved SE categories
- for polyA soft-clipping, use "min_soft_clip_PolyA_base_frac_for_converion": 0.8
- adding extract_reads_from_bam.py util
- transcript_i cannot subsume contained/compatible transcript_j if transcript_i has expression < transcript_j.  Also recomputing exon coverage stats after TSS splits
- similarly reassign coverage for polyA-split exon segments after splitting
- including length dist plotter in sqanti-like
- new classify_read_alignments_to_SQANTI_cats.py that works with both bams and gtfs as input
- in quant-only mode, return the original annot_gtf provided


v0.2.35
- for alignment correction, set "min_softclip_realign_test": 5
- including exon spur removal during quant-only splice graph building
- added gtf-to-bed converter for improved IGV and ITV usage
- can build count and expr matrices for splice patterns
- adding stand-alone annotate_bam_with_read_tracking_info.py


v0.2.34
- refined sc gtf merge and expr quant merging: performing per-cluster quant and merging tracking files plus providing cluster-based pseudo-bulk expr files.
- only report read weighting and zero-frac-assigned compat reads under debug mode instead of by default
- use fixed min_reads_novel_isoform during sc merge gtf across clusters mode

v0.2.33
- make clear in sc wdl that expr is pseudobulk
- enable gene symbol incorporation into single cell data when using ref-quant only mod
- in quant-only w/ LRAA input gtf having TSS and PolyA annotations, only integrate those TSS and PolyA features instead of trying to de novo discover from read alignments

v0.2.32
- incorporating single cell cluster-based assembly workflow

v0.2.31
- rare edgecase bugfix around multipaths that should have been single paths

v0.2.30
- major refactoring of the LRAA driver
- doing a full quant post asm including a final round of min isoform fraction based filtering

v0.2.29
- bugfix around retaining spurs matching to input transcripts
- added another ONT test

v0.2.28
- running most of assembly mode in draft quant mode then employing the final quant mode - difference is how they priority FSM assignments of reads.
-  if more than 10 isoforms for a given gene, then be a bit more aggressive in filtering based on isoform fractional expression to reduce runtimes
- set max number of graph path nodes per component


v0.2.27
- incorporate_gene_symbols_in_sc_features.py for sc matrices
- support gzipped gtf files as input
- address inefficiency in resetting read count
- no pruning of exon coverge segments that overlap exons of input reference transcript

v0.2.26
- pruning introns > 100kb
- default threads=4
- norm default now 1k

v0.2.25
- bugfix around LowFi aggregation of splices and intron pruning

v0.2.24
- added code to infer transcriptional orientation of reads not provided as strand-specific. Not fully baked inot the LRAA pipeline yet though.
- under LowFi mode, setting fracture_splice_graph_at_input_transcript_bounds to False, also added contaiment and contained-by info in the expr tsv
- include exon and intron coords in the expr file
- set min intron support to 2 - should except input guide transcripts though
- no purging of input gtf introns
- aggregate splice dist in LowFi mode
- in debug mode, report prelim isoforms before filtering
- added debug option to not filter isoforms, and also now only reporting read support in gtf file if debug set


v0.2.23
- set min path score default to 1 instead of 2

v0.2.22
- added option --LowFi 

v0.2.21
- under assembly mode, include alt isoform splice compatibilities in quant outputs.
- adjusted default requirements for polyA and TSS definitions to slightly more stringent: min_alignments_define: 5, min_frac_gene_alignments_define: 0.1

v0.2.20
- merge short deletions in input transcripts

v0.2.19
- high EM max iter during asm
- EM alpha set to zero during asm except for final quant

v0.2.18
- including EM regularization factor EM_alpha * num_ambigous_reads, used during quant but not asm step.
- capping EM steps at 250 for quant
- default min mapQ is now zero

v0.2.17
- incorporated FSM compatibility into read assignment logic cascade.


v0.2.16
- wdls greatly simplified.
- after initial asm, doing quant based on all reads not just the norm reads


v0.2.15
- altered handling of input ref trans in genome annot guided mode. Added CLI option: --ref_trans_filter_mode, default:  retain_expressed

v0.2.14
- incorporated CGPT implementation of EM as default and alternative to the BJH implementation. Can be set via CLI --EM_implementation

v0.2.13
- major refactoring of the EM code

v0.2.11
- redo read weighting, restrict to initial M step.

v0.2.10
- dont aggregate small splice dist if alt intron is a ref intron
- computing multipath transcript compatibility weights based on distance to termini
- restructuring quant in assembly step to access mp_counter
- included option --no_use_weighted_read_assignments to restore earlier behavior

v0.2.9
- made aggressive read assignment a configurable parameter
- skip genes w/o read support
- adding test for expr corr w/ and w/o EM
- blackened all pylib
- adjusting TSS and PolyA for input_transcripts based on dist/2

v0.2.8
- split parallelization targeting larger components to separate threads while the main process quickly handles the smaller ones directly.
- wdl updates exposing more input parameters
- refactoring of Quant module, running EM on per-gene basis instead of per-contig, separate EM max iterations for quant vs. asm
- refactoring of transcript isoform filtering methods into TranscriptFiltering module.

v0.2.7
- pruning low supported introns within exon islands and aggregating for splice dist for low qual alignments

v0.2.6
- during assembly mode, including input transcripts as substrates for path nodes instead of just for the splice graph construction

v0.2.5
- EM on by default (use --no_EM to disable)
